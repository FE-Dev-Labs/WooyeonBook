import styles from '@/styles/common/loading.module.css';

export default function Loading() {
	return (
		<div className={styles.container}>
			<div className={styles.wrapper}>로딩~~~~~~~~~~~~~~~~~~~~~~</div>
		</div>
	);
}
